# vi: set ft=sql :

<both>
    create table B
    (
        id integer auto_increment
    ,   name varchar(32)
    
    ,   primary key (id)
    );

<old>
    insert into B (name) values ('abc');

<new>
    insert into B (name) values ('def');

<expected>
    update `B` set `name`='def' where `id`=1;
